# Japan Disaster Alert - Backend Environment Configuration
# Copy this file to .env and set your values:
#   cp .env.example .env

# -------------------------------------------------------------------
# Environment
# -------------------------------------------------------------------
ENVIRONMENT=development
LOG_LEVEL=INFO

# -------------------------------------------------------------------
# External API timeouts (seconds)
# -------------------------------------------------------------------
API_TIMEOUT=10.0

# -------------------------------------------------------------------
# AI Provider
# auto  : prefer Gemini, fall back to Claude
# gemini: use Gemini only
# claude: use Claude only
# Leave both API keys unset to use the built-in static translation
# table (75 location names x 15 languages) without any AI calls.
# -------------------------------------------------------------------
AI_PROVIDER=auto

# Gemini API (recommended)
# Get your key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# Claude API (optional fallback)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -------------------------------------------------------------------
# CORS (comma-separated list of allowed origins)
# In production, replace with your actual frontend URL
# -------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8000

# -------------------------------------------------------------------
# Server
# -------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000
